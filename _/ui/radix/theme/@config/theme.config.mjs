import { buildThemePackage } from "@repo/tw-theme/config"

export default buildThemePackage('@repo-theme/radix')

.colors(({hsl, rgb, hex}) => ({



}))

// .variants(({hsl, rgb, hex}) => ({

//     neo: {

//     }
    
// }))

.build()



